<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.onlineclass.mapper.QuizMapper">

    <select id="getMyQuiz" resultType="java.util.HashMap">
        select *
        from (select *
              FROM quiz_submit
              where quiz_submit.q_id in (select q_id FROM quiz where teaching_id = #{tid}) and u_id = #{uid}) t
                 right join quiz on t.q_id = quiz.q_id
    </select>
</mapper>
